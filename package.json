{
  "name": "flint-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.2",
  "scripts": {
    "docs:dev": "pnpm --filter flint-docs docs:dev",
    "docs:build": "pnpm --filter flint-docs docs:build",
    "docs:preview": "pnpm --filter flint-docs docs:preview",
    "vscode:package": "pnpm --filter flint-vscode package"
  }
}
